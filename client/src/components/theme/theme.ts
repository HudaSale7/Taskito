import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1f262e",
      light: "#313a45",
      contrastText: "#9FADBC",
    },
    secondary: {
      main: "#90caf9",
    },
  },
});
